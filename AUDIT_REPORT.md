# JShell 项目安全与质量审计报告

审计日期：2026-07-29
审计范围：`D:\Git\ashell` 的 Rust 源码、依赖、配置持久化、SSH/SFTP 连接链路、代理逻辑、错误处理及本地质量检查
范围约束：发布文件仅审阅，不修改 `.github`、Release 工作流或发布脚本

## 1. 执行摘要

本轮已按 P0、P1、P2 顺序完成主要安全和操作逻辑修复：

1. SSH 与 SFTP 不再无条件接受服务器主机密钥，统一执行严格的 `known_hosts` 校验。
2. 本地配置不再使用机器标识作为长期加密秘密；随机主密钥由操作系统安全存储管理，不生成保存明文主密钥的 `.gen` 文件。
3. 配置迁移、代理选择和持久化配置加载均改为失败关闭，避免迁移失败后继续运行、代理配置错误后静默直连或覆盖原配置。
4. HTTP CONNECT 响应、代理优先级和连接期间的配置快照已修正，并增加专项测试。
5. 配置写入改为同目录暂存、同步并原子替换，降低崩溃或断电造成配置截断的风险。
6. `cargo audit` 发现的漏洞由最初 10 个降至 1 个；维护性警告由 12 个降至 7 个。
7. 127 项测试、全目标检查、严格 Clippy 和格式检查均通过。

当前没有未处理的 P0。剩余安全漏洞为 `rsa` 的中危计时侧信道公告，该依赖目前没有修复版本；保留它是为了继续支持常见的 SSH RSA 密钥和服务器。其余 7 项均为固定 GPUI/组件依赖链中的“停止维护”警告，不是已确认的可利用漏洞。

## 2. 风险发现与修复

### P0/P1 - SSH 和 SFTP 跳过主机密钥校验（已修复）

原实现中的 SSH 和 SFTP `check_server_key` 均无条件返回成功，没有读取或验证 `known_hosts`。攻击者可通过中间人攻击伪装远程服务器；密码认证可能泄露密码，所有认证方式均可能遭到终端输出、命令结果或 SFTP 文件篡改。

已完成：

1. 新增 `src/session/host_keys.rs` 作为共享校验模块。
2. SSH 与 SFTP 在身份认证前统一读取 `<用户目录>/.ssh/known_hosts`。
3. 支持普通主机、非默认端口、通配符、否定模式、哈希主机名和撤销标记。
4. 未知主机、密钥变化、撤销密钥、文件缺失和解析失败均默认拒绝连接。
5. 增加匹配、未知、变化、撤销、哈希主机、大小写和非默认端口测试。

当前策略不会自动信任或写入未知主机。首次连接前需要通过可信渠道核对指纹并准备 `known_hosts` 记录；这会牺牲少量首次连接便利性，但避免把 P0 修复退化成未经确认的 TOFU 自动信任。

### P1 - 本地敏感配置使用机器标识派生密钥（已修复）

旧版本从 `MachineGuid`、`machine-id` 等非秘密机器标识派生 XChaCha20-Poly1305 密钥。配置中包含 SSH 密码、私钥内容、私钥口令和代理密码；配置文件与机器标识同时泄露时，攻击者可能恢复这些凭据。

已完成：

1. 新增 `src/session/config_key.rs`，首次运行生成 32 字节随机主密钥。
2. 通过 Windows Credential Manager、macOS Keychain 或 Linux Secret Service 保存主密钥。
3. 新配置格式升级为版本 2，使用随机 nonce 和 XChaCha20-Poly1305；机器标识与 Argon2 仅保留用于读取版本 1 并执行一次性迁移。
4. 版本 2 配置缺少主密钥、系统安全存储被锁定或读取失败时，不创建替代密钥，也不覆盖现有配置。
5. `MasterKey` 的调试输出固定为 `[REDACTED]`。
6. 不生成 `.gen`、旁路密钥文件或其他明文主密钥副本，因此不影响普通用户开箱启动；前提是桌面系统的凭据库可用。

### P1 - 代理错误时静默直连及连接期间重复加载配置（已修复）

原逻辑会在每次连接时重新执行 `ConfigStore::load()`。代理类型未知、主机为空、端口缺失或持久化配置加载失败时，部分路径会静默改为直连；这既可能绕过用户的网络边界，也会让状态文本、SSH、SFTP 和远程文档使用不同的配置时点。

已完成：

1. 主窗口加载配置后生成 `ConnectionProxyConfig` 内存快照，并显式传给 SSH、SFTP 和远程文档连接。
2. 连接期间不再读取磁盘配置。
3. 优先级固定为：会话代理、环境代理、全局代理、经确认的直连。
4. 已选择的代理类型、主机或端口无效时直接返回错误，禁止静默直连。
5. 持久化配置加载失败后进入非持久化模式，并拒绝未经确认的直连。
6. 旧会话缺少 `proxy_type` 时默认继承代理策略，不再错误地默认为 SOCKS5。
7. 环境代理只接受 `socks5://`、`socks5h://` 和 `http://`；未知协议以及当前不支持的 `https://` 代理均明确报错。
8. HTTP CONNECT 使用 `httparse` 校验真实 2xx 状态码，限制响应头为 16 KiB，并通过 `BufStream` 保留响应头后可能已到达的 SSH 握手字节。
9. 增加代理优先级、严格失败、环境变量解析、IPv6 authority 和 HTTP 状态码测试。

### P1/P2 - 安全迁移失败后仍可能继续运行或遗留明文（已修复）

版本 1、明文配置和旧目录位置都属于安全敏感迁移。若迁移写入失败却继续启动，后续运行可能误以为迁移已经完成；从旧目录迁移后若保留源文件，还会留下包含敏感信息的旧副本。

已完成：

1. 版本 1、明文格式或旧目录迁移的保存失败会完整上抛，启动层进入非持久化模式并显示本地化错误通知。
2. 迁移保存失败时保持原文件字节不变。
3. 从旧配置目录成功迁移后删除旧源文件，避免遗留明文或旧加密副本。
4. 字体偏好等非安全迁移仍采用告警而不阻断启动。
5. 增加迁移保存失败、原文件保持不变和旧源文件清理测试。

### P2 - 任意解密错误回退为明文配置（已修复）

原逻辑在解密失败后尝试把整个文件作为 JSON 配置读取，扩大了明文凭据被长期接受和伪装信封被误解析的风险。

已完成：

1. 加载前先识别加密信封版本，不再把任意解密错误当作明文。
2. 只有包含已知配置字段且不包含信封字段的旧 JSON 才允许一次性迁移。
3. 损坏、未知版本或伪装为信封的 JSON 返回错误，并保持原文件不变。
4. 合法明文旧配置迁移成功后立即以版本 2 加密格式重写。

### P2 - 配置保存不是原子操作（已修复）

原保存逻辑直接覆盖目标文件，程序崩溃、断电或磁盘错误可能产生截断配置。

已完成：

1. 两个保存入口统一使用同目录 `NamedTempFile` 暂存。
2. 完整写入后执行 `flush` 和 `sync_all`，再原子替换目标。
3. Unix 下暂存文件权限设为 `0600`，替换后同步父目录。
4. 替换失败返回分阶段错误链、清理暂存文件并保持旧配置字节不变。
5. 增加成功替换和模拟替换失败测试。

### P2 - 依赖漏洞与过期锁定版本（大部分已修复，存在已知残留）

主要升级与约束如下：

| 依赖 | 处理 |
|---|---|
| `russh` / `russh-cryptovec` | `0.49.2/0.48.0` 升至 `0.60.3`，并完成认证 API 迁移 |
| `time` | `0.3.45` 升至 `0.3.47`；项目最低 Rust 版本相应设为 `1.88.0` |
| `quinn-proto` | `0.11.14` 升至 `0.11.15` |
| `crossbeam-epoch` | `0.9.18` 升至 `0.9.20` |
| `wayland-scanner` | `0.31.10` 升至 `0.31.11` |
| `zbus_xml` | `5.1.1` 升至 `5.2.1` |
| `xcb` | 固定到官方修复提交 `ae3b2cd080e78173223038ccbebdc644eaf4a9ad`，移除有问题的 `quick-xml 0.30.0` |
| `portable-pty` | `0.8.1` 升至 `0.9.0`，移除停止维护的 `serial` 依赖 |
| `spin` | 撤回的 `0.9.8/0.10.0` 升至未撤回的 `0.9.9/0.10.1` |
| `anyhow` / `memmap2` / `bitflags` | 升至当前兼容补丁版本 |

`russh` 关闭默认 `aws-lc`，改用 `ring`、`flate2` 和 `rsa` 功能，避免 Windows 开箱构建额外依赖 NASM，同时保留 SSH RSA 兼容性。

### P2 - 严格 Clippy 检查失败（已修复）

已处理 Rust 1.97 报出的冗余绑定、复杂类型、过多参数、无意义 unit 表达式和可折叠条件等问题；共享上下文通过现有局部类型收拢，其余为等价的机械改写。当前严格命令已通过：

```text
cargo clippy --all-targets --all-features -- -D warnings
```

### P1 - Release 工作流二进制名称不一致（仅记录，按范围不修复）

`Cargo.toml` 的包和默认二进制名称为 `jshell`，而 `.github/workflows/release.yml` 的部分步骤仍复制 `ashell` 或 `ashell.exe`。干净 CI runner 可能因此找不到产物。该问题属于发布流程，按本轮约束仅保留审计记录，未修改 `.github`、Release 工作流或发布脚本，也未执行 Release 构建。

## 3. `cargo audit` 结果

最终扫描 1029 个锁定依赖：

- 已确认漏洞：1 个。
- 维护性警告：7 个。
- 命令退出码：1，原因是仍存在下述无修复版本漏洞，不代表扫描未执行。

### 已知漏洞

| 项目 | 结果 |
|---|---|
| 公告 | `RUSTSEC-2023-0071` / `CVE-2023-49092` |
| 依赖 | `rsa 0.10.0-rc.16`，由 `russh 0.60.3` 引入 |
| 严重度 | CVSS 5.9，中危 |
| 影响 | RSA 私钥运算的非恒定时间实现可能泄露计时信息 |
| 修复状态 | RustSec 明确标记为没有可用修复版本 |
| 当前决策 | 保留 RSA 支持，避免破坏常见 RSA 私钥和服务器兼容性；优先建议使用 Ed25519 密钥 |

若后续产品明确只支持 Ed25519/ECDSA，可以关闭 `russh` 的 `rsa` 功能并移除此公告；这属于兼容性策略变更，不应仅为获得“零告警”而静默执行。

### 维护性警告

| 警告 | 依赖来源与处理建议 |
|---|---|
| `async-std 1.13.2` | GPUI 的 `http_client -> async-tar` 链路；跟随 GPUI 升级 |
| `instant 0.1.13` | `gpui-component` / `notify` 链路；跟随组件升级 |
| `paste 1.0.15` | `gpui-component` 与图像栈链路；跟随上游替换 |
| `proc-macro-error2 2.0.1` | GPUI 的 `stacksafe 0.1.4` 链路；同时产生未来 Rust 兼容提示 |
| `rustls-pemfile 2.2.0` | `gpui-component-assets -> zed-reqwest` 链路 |
| `rustybuzz 0.20.1` | GPUI 的 `usvg/resvg` 字体与 SVG 栈 |
| `ttf-parser 0.25.1` | GPUI 的字体与 SVG 栈 |

这些警告均不是项目直接调用层可以独立替换的依赖。强行补丁可能破坏固定的 GPUI 与 `gpui-component` API 组合；本轮选择记录并跟随上游，而不是引入未经验证的框架级升级。

## 4. 操作逻辑与调试可观测性

1. 启动时只加载一次持久化配置；失败会记录完整错误链，界面显示本地化通知，并使用明确的非持久化模式。
2. 非持久化模式不会把当前内存配置写回磁盘，也不会在连接层偷偷重新加载配置。
3. SSH、SFTP 和远程文档共享同一代理快照和主机密钥策略，状态文本与真实连接路径保持一致。
4. 代理日志只记录类型、主机、端口和用户名，不记录代理密码。
5. HTTP CONNECT 失败返回真实状态码；无效环境代理会保留错误原因，不再猜测协议。
6. SFTP 编辑临时文件不再通过重新加载加密配置来解析目录，目录异常时回退到操作系统临时目录，也不再使用可能崩溃的 `unwrap()`。
7. 安全迁移、原子替换、凭据库读取和连接失败均使用带上下文的错误链，便于日志定位具体阶段。

## 5. 验证结果

| 检查 | 最终结果 |
|---|---|
| `cargo fmt --check` | 通过 |
| `cargo test --quiet` | 通过：127 成功，0 失败，0 忽略 |
| `cargo check --all-targets` | 通过；仅有 `proc-macro-error2 2.0.1` 上游未来兼容提示 |
| `cargo clippy --all-targets --all-features -- -D warnings` | 通过 |
| `cargo audit` | 已执行：1 个无修复版本漏洞，7 个维护性警告 |
| `git diff --check` | 通过；仅显示 Windows 工作区的 LF/CRLF 转换提示 |
| `git diff --name-only -- .github scripts` | 无输出，发布目录未修改 |
| `git status --short --branch` | 工作树干净，当前分支已跟踪远端同名分支 |
| `cargo build --release` | 按“不参与发布流程”的范围约束未执行 |

## 6. 当前残留问题

1. `RUSTSEC-2023-0071`：无修复版本的 RSA 中危公告。当前建议默认新建 Ed25519 密钥，并持续跟踪 `russh`/RustCrypto 更新。
2. 7 个固定上游依赖的停止维护警告：需要在 GPUI 和 `gpui-component` 可一起升级时重新评估。
3. `proc-macro-error2 2.0.1` 未来 Rust 兼容提示：当前 Rust 1.97 仍可构建，但上游升级前可能在未来工具链变成硬错误。
4. Release 工作流中的 `jshell`/`ashell` 命名不一致：已记录，按约束不处理。
5. 首次 SSH/SFTP 连接需要已有可信 `known_hosts` 记录；当前尚无应用内指纹确认与安全写入流程。

除上述明确残留外，本轮静态检查、单元测试和依赖扫描未发现新的 P0/P1 安全问题。

## 7. 审计限制

- 未连接真实 SSH/SFTP 服务器执行密码、密钥、代理、恶意主机密钥和文件传输端到端测试。
- 系统安全存储通过可注入实现完成单元测试，尚未分别在 Windows Credential Manager、macOS Keychain 和 Linux Secret Service 上执行真实端到端验证。
- `cargo check --all-targets` 覆盖当前 Windows 主机的所有 Cargo 目标，不等同于 Linux/macOS 交叉编译。
- 未执行 Release 构建、安装包和发布产物验证。
- 审计基于当前工作树；用户保留的未跟踪目录和历史设计文档未删除、未回滚，也未纳入安全修复提交范围。

## 8. 提交状态

本轮修改已按设计记录、安全修复、审计报告和忽略规则分批提交，并推送到 `origin/codex/windows-titlebar`。项目工作目录已迁移至 `D:\Git\ashell`。
