# JShell 测试计划（v1.0）

- 适用版本：v1.0.0（及后续版本）
- 测试依据：`README.md` 功能说明（需求基线）、`AUDIT_REPORT.md` 与 `docs/RELEASE_AUDIT_*.md`（历史审计与已知风险）、`docs/superpowers/specs/`（设计规格）
- 自动化现状：368+ 项内联单测（`cargo test --bin jshell`）、CI 质量门禁（fmt / test / check / clippy `-D warnings` / cargo audit `--deny warnings`）
- 执行工具：`scripts/verify.sh`（本地全量门禁）、`scripts/smoke-linux.sh`（Linux 冒烟）
- 本文档覆盖六类测试：功能、性能、兼容性、安全、回归、易用性。每类给出目标、用例清单（含自动化覆盖标注）、判定标准与执行频率。

---

## 1. 功能测试

**目标**：验证各功能点符合 README 需求与设计规格。纯逻辑以自动化单测覆盖；依赖 GUI/真实远端的功能以手工用例覆盖。

**自动化覆盖标注**：`[A]` 已有自动化测试；`[A+]` 本次新增自动化；`[M]` 手工用例（发布前执行）。

### 1.1 会话管理（`src/session/`、`src/app/dialogs.rs`）

| 编号 | 用例 | 预期 | 覆盖 |
|---|---|---|---|
| F-001 | 新建/编辑/删除/克隆 SSH 会话并持久化到 `sessions.json` | 重启后会话保留；编辑不丢字段 | [A] `session/config.rs` 序列化往返测试；[M] GUI 表单流程 |
| F-002 | 会话文件夹：创建、重命名、批量连接、保存 | 文件夹层级正确持久化 | [M] |
| F-003 | 同一会话重复打开复用既有标签 | 不产生第二个连接标签 | [A] `connected_session_tab_id`；[M] 双开验证 |
| F-004 | 断线后原地重连，陈旧后端输出被抑制 | 旧 generation 事件不写入新会话 | [A] `terminal/mod.rs` generation 守卫 |
| F-005 | 端口/表单校验（非 0、1-65535） | 非法输入被拦截 | [A] `parse_non_zero_u16`；[M] 表单 |
| F-006 | `~/.ssh/config` 解析：Host/HostName/User/Port/IdentityFile、`=` 与 tab 分隔、注释、大小写 | 字段正确；Match/Include 安全忽略 | [A+] `ssh_config.rs` 14 项测试 |
| F-007 | SSH 认证四模式：密码、密钥文件、内联密钥、ssh-config | 各模式连通；默认密钥尝试顺序正确 | [A] `ssh_keys.rs`；[M] 真实主机 |
| F-008 | 代理：无/直连/继承/自定义（SOCKS5/SOCKS5H/HTTP/HTTPS） | 按策略解析、校验并建立通道 | [A] `config.rs` 代理解析测试（含 rcgen TLS）；[M] 真实代理 |

### 1.2 终端（`src/terminal/`、`src/backend/`）

| 编号 | 用例 | 预期 | 覆盖 |
|---|---|---|---|
| F-010 | 本地终端（`$SHELL`，Windows 回退 powershell） | 交互正常 | [M] |
| F-011 | SSH 终端连接与交互 | 命令执行、输出回显正确 | [M] 非生产主机 |
| F-012 | 串口会话（波特率校验、CR 插入、虚拟口回退） | 参数异常有回退不崩溃 | [A] `serial.rs`；[M] 真实串口 |
| F-013 | 粘贴/选择/右键复制、鼠标选择 | 剪贴板行为正确 | [M] |
| F-014 | 终端搜索与高亮定位 | 匹配高亮、上下跳转 | [M] |
| F-015 | Ctrl+点击 URL 打开（仅 http/https 且边界正确） | 正确解析、跨行换行 URL 可解析 | [A+] `highlight.rs` URL 测试；[M] |
| F-016 | 日志关键词/状态码/IP/端口高亮 | 大小写不敏感、不重叠、边界正确 | [A+] `highlight.rs` 10 项测试 |
| F-017 | 命令历史拉取（bash/zsh/fish）与补全 | 解析正确、去重 | [A] `command_history.rs` |

### 1.3 SFTP 文件管理（`src/sftp/`）

| 编号 | 用例 | 预期 | 覆盖 |
|---|---|---|---|
| F-020 | 浏览/刷新/隐藏文件开关 | 列表正确 | [M] |
| F-021 | 上传/下载（单文件、目录 tar.gz 归档） | 完整性（大小一致）、进度显示 | [A] 传输状态测试；[M] |
| F-022 | 暂停/恢复/取消传输 | 状态机正确，取消不产生半成品 | [A] `TransferStateFlag` 测试 |
| F-023 | mkdir/删除（含多选、危险系统路径警告） | 提示与结果正确 | [M]；[A] `shell_quote`（远程命令安全引用） |
| F-024 | 目录归档解包：正常条目、`..` 穿越条目、符号链接逃逸 | 正常解包；穿越被跳过；逃逸被拒绝 | [A+] 4 项解包回归测试 |
| F-025 | 静默断线检测与指数退避重连、手动重连、永久错误（认证/主机密钥）停止重试 | 状态图标与提示正确 | [A] `sftp_retry_policy`；[M] 断网演练 |
| F-026 | 终端 CWD 跟随（关闭/实时/切标签时） | 按所选模式跟随目录 | [A] `cwd_follow.rs` 状态机 |

### 1.4 远程编辑器（`src/document/`）

| 编号 | 用例 | 预期 | 覆盖 |
|---|---|---|---|
| F-030 | 双击打开远程文件、编辑、保存（临时文件+备份+原子替换） | 保存成功、权限保留 | [A] 控制器状态流；[M] 真实主机 |
| F-031 | 保存冲突检测（远端已变更） | 提示冲突，需显式覆盖 | [A] `run_save_check`；[M]（beta.2 未验证项，v1.0 前必须） |
| F-032 | 保存结果未知（断线） | 保留脏状态、不盲目重试 | [A] `WriteFailureDisposition`；[M] 断网演练 |
| F-033 | 远端删除/断线后不自动保存 | 提示而非静默重建 | [M] |
| F-034 | 大文件分页只读、二进制/非 UTF-8 保护 | 不崩溃、提示只读 | [A] `large_file.rs`；[M] |
| F-035 | 分离窗口/重新吸附、全局软换行 | 窗口状态正确 | [M] |

### 1.5 同步（`src/sync/`、`src/app/config_sync.rs`）

| 编号 | 用例 | 预期 | 覆盖 |
|---|---|---|---|
| F-040 | WebDAV/S3/R2 上传下载、etag 冲突处理 | 冲突不静默覆盖 | [A] `sync/mod.rs`、`config_sync.rs` 29+27 项 |
| F-041 | 可移植载荷白名单（不含本机敏感字段） | 载荷仅含白名单字段 | [A] `payload.rs` |
| F-042 | 凭据持久化/清理（keyring） | 密钥环条目正确 | [A] `credential_store.rs` |
| F-043 | 真实 R2 对象上传/下载/条件覆盖 | 与本地模拟一致 | [M]（beta.2 未验证项，需 R2 测试账户） |

### 1.6 托盘 / 单实例 / 信号（`src/app/`）

| 编号 | 用例 | 预期 | 覆盖 |
|---|---|---|---|
| F-050 | 关闭转托盘、托盘显示/退出、设置开关 | 状态保持、开关生效 | [M]；[A] `system_tray` 配置默认值 |
| F-051 | 双实例：第二实例通知并立即退出 | 首实例保持运行 | [A] 逻辑测试；[M] `scripts/smoke-linux.sh` |
| F-052 | SIGTERM/SIGINT 优雅退出（3s 内、退出码 0、配置落盘） | 完整退出流程含未保存提示 | [M] `scripts/smoke-linux.sh` |
| F-053 | 二次信号强制终止 | 处理器恢复默认处置 | [A] 审计修复复验 |

### 1.7 主题 / i18n / 键位 / 系统监控

| 编号 | 用例 | 预期 | 覆盖 |
|---|---|---|---|
| F-060 | 三套主题+跟随系统、字体/字号持久化 | 切换即时生效 | [A] `theme.rs` 解析；[M] |
| F-061 | 中英文切换、系统语言跟随 | 文案完整无缺键 | [A+] locale 键对齐测试（en/zh-CN 键集一致、值非空） |
| F-062 | 键位重绑与录制器 | 持久化、冲突提示 | [A] recorder 动作表；[M] |
| F-063 | 系统监控（CPU/内存/交换/网络/磁盘）、远端探针解析 | 数据合理、异常不崩溃 | [A] `remote_snapshot_from_kv`；[M] |

**判定标准**：上述 [A]/[A+] 全部通过；[M] 用例在发布前按《发布就绪清单》（`docs/RELEASE_READINESS.md`）逐项执行并记录结果。

---

## 2. 性能测试

**目标**：确认交互流畅、内存受控、长时稳定。当前无自动化基准，采用手动测量规程；纯逻辑热点（高亮正则、加解密、known_hosts 解析）为后续 criterion bench 候选，本次不引入依赖。

**测量环境**：`cargo build --release` 产物；记录 OS/CPU/内存/GPU。工具：系统监视器 + `time` + 自测脚本。

| 编号 | 指标 | 步骤 | 目标阈值 | 基线（待回填） |
|---|---|---|---|---|
| P-001 | 启动时间 | 冷启动到窗口可交互（托盘/单实例初始化完成后） | < 2s | — |
| P-002 | 大输出终端帧率 | `yes` / `cat /dev/zero` 持续输出 60s，观察滚动流畅度与 CPU | 无明显掉帧；CPU 单核 < 60% | — |
| P-003 | scrollback 内存 | 10 万行输出后驻留内存增量 | < 300MB 增量 | — |
| P-004 | 大文件编辑器 | 打开 200MB 文本，分页翻页延迟 | 翻页 < 200ms，内存受控 | — |
| P-005 | SFTP 批量传输 | 1000 个小文件 + 1 个 1GB 大文件上传/下载吞吐 | 无内存线性增长；吞吐接近链路带宽 | — |
| P-006 | 多标签 16ms 事件泵 | 8 个活动 SSH 标签同时输出 | UI 事件延迟 < 16ms，泵无积压 | — |
| P-007 | 配置加载/加密 | 100 会话配置冷加载、v2 加解密耗时 | 加载 < 100ms | — |
| P-008 | 长时稳定性 | 连续运行 24h（含托盘驻留、SSH 空闲保活、日志轮转） | 无泄漏趋势（RSS 波动 < 10%） | — |

**判定标准**：所有指标在阈值内；超出项在 `docs/RELEASE_READINESS.md` 记录并评估是否阻塞。每次发布前执行 P-001/P-002/P-004；大版本前执行全部。

---

## 3. 兼容性测试

**目标**：三平台 × 窗口系统 × 架构矩阵下功能一致。

| 平台 | 环境 | 必测项 |
|---|---|---|
| Windows | 10 / 11（x86_64） | 自定义标题栏与窗口控制、托盘左键恢复、命名管道单实例、`windows_subsystem` 无控制台、任务栏/开始菜单行为 |
| macOS | aarch64 / x86_64（ad-hoc 签名） | 红绿灯定位、`QuitMode::LastWindowClosed`、登录 shell 环境变量同步（代理变量）、NSStatusItem 托盘、Gatekeeper 提示（文档化） |
| Linux | X11（GNOME/KDE）、Wayland（GNOME/KDE） | GTK 托盘创建、appindicator 左键无响应（已知限制，菜单代替）、Wayland 最小化被忽略（GNOME，安全回退）、glibc 基线（ubuntu-22.04 构建产物） |

**专项**：
- C-001 旧版 `ashell` 配置迁移：`~/.config/ashell/sessions.json` → 迁移后删除源（[A] 迁移测试）
- C-002 配置前向兼容：未知字段/缺省字段以 serde 默认值加载不崩溃（[A] serde 默认测试）
- C-003 已知平台风险回归（`AUDIT_REPORT.md` 第 5 节）：每发布至少复核一次
- C-004 CI 四平台 release 构建通过（自动化）；平台矩阵当前只构建，建议后续补 `cargo test`（见 RELEASE_READINESS 优化项）

**判定标准**：矩阵内无功能性阻塞；已知限制必须与文档一致。

---

## 4. 安全测试

**目标**：无凭据泄露、无注入、无越权写入、依赖无漏洞。

### 4.1 自动化（CI 常驻）
- `cargo audit --deny warnings`：0 漏洞；10 条「unmaintained」警告已按 `.cargo/audit.toml` 豁免（GTK3 托盘链，见注释与 `AUDIT_REPORT` 第 5 节）
- `cargo clippy -D warnings`、`cargo fmt --check` 常驻
- 新增安全回归测试（本次）：
  - S-001 目录归档解包：tar/tar.gz `..` 条目跳过、符号链接逃逸拒绝、zip 正常解包 [A+]
  - S-002 `shell_quote` 对抗输入（引号、换行、`$(...)`、反引号）单引号上下文不可逃逸 [A+]
  - S-003 locale 键对齐与空值检查 [A+]（防缺键回退显示键名）
- 既有：host-key 变更/撤销/排除拒绝且不修改 known_hosts（26 项）、密钥环锁定 fail-closed、v1→v2 迁移删源、同步载荷篡改/超限/重复 id 拒绝、密钥 Debug 脱敏、`zeroize` 使用

### 4.2 人工渗透/审计清单（发布前）
| 编号 | 检查项 | 依据位置 |
|---|---|---|
| S-010 | TOFU 主机密钥首次自动接受——确认是否需"提示确认"设置项（设计文档已记录该取舍） | `session/host_keys.rs`、`docs/superpowers/specs/2026-08-01-*` |
| S-011 | v1 加密硬件 UUID 兜底常量公开可知——评估 fail-closed | `session/config.rs:2213` |
| S-012 | HTTP 代理 Basic 凭据仅经 CONNECT 隧道发送（`http://` 代理为明文，文档化） | `session/config.rs:1951` |
| S-013 | 日志脱敏抽查：不得出现密码/密钥/代理凭据 | `~/.config/jshell/log` |
| S-014 | 配置目录/日志目录权限 0700（本次已收紧日志目录） | `session/config.rs`、`app/startup.rs` |
| S-015 | 远程命令执行路径复核：`tar`/`rm` 命令均经 `shell_quote`，300s 超时 | `sftp/mod.rs` |
| S-016 | 真实主机 host-key 变更/撤销全流程演练（非生产 Linux SSH/SFTP 主机） | beta.2 未验证项 |

**判定标准**：4.1 全部通过；4.2 全部执行并记录结论，阻塞项见 RELEASE_READINESS。

---

## 5. 回归测试

**目标**：改动不破坏既有行为；368+ 项单测为回归基底。

- **触发策略**：每次提交前本地跑 `scripts/verify.sh`；PR/推送由 CI 全量执行；发布前追加 `scripts/smoke-linux.sh`。
- **重点回归面**（历史缺陷多发区）：
  - 重连 stale 事件 generation 守卫（SSH/SFTP）
  - 断线后不重放破坏性 SFTP 命令（上传/下载/删除）
  - 保存结果未知状态机（`WriteFailureDisposition`）
  - 托盘/单实例/信号：双实例退出、二次信号逃生、跨用户套接字隔离
  - 同步合并本地字段保护（`system_tray` 等 UI 状态不被远端覆盖）
- **发布审计衔接**：每次发布沿用 `AUDIT_REPORT` 流程（门禁结果 + 冒烟记录 + 已知风险更新）；模板化建议见 RELEASE_READINESS。

**判定标准**：全量测试通过 + 冒烟通过；任何红项阻塞发布。

---

## 6. 易用性测试

**目标**：界面直观、交互顺畅、信息可达。

### 6.1 手工评估清单（发布前）
| 编号 | 检查项 | 通过标准 |
|---|---|---|
| U-001 | 首次启动：无配置时进入主页，引导创建会话 | 30s 内无文档即可完成首个本地会话 |
| U-002 | 会话/设置/传输对话框：Tab 顺序、必填校验提示 | 键盘可完成全流程 |
| U-003 | 窄窗口 960×700 布局无遮挡、可滚动 | 功能可达（beta.2 已验，回归确认） |
| U-004 | 标签颜色/状态语义（终端/文档/系统、连接/保存/错误） | 状态一眼可辨 |
| U-005 | 托盘可发现性：Windows/macOS 左键恢复；Linux 菜单显示/隐藏（左键为上游限制） | 行为与文档一致 |
| U-006 | 中英文文案完整、术语一致（缺键由自动化拦截） | 无键名回退显示 |
| U-007 | 错误提示可读可行动（连接失败、保存冲突、同步冲突） | 提示含下一步动作 |

### 6.2 自动化
- [A+] locale 键对齐测试：en/zh-CN 键集一致、值非空
- [A] 主题解析、字体回退、布局状态持久化测试

**判定标准**：U-001~U-007 全部通过；自动化全绿。

---

## 附：执行频率汇总

| 类型 | 频率 | 载体 |
|---|---|---|
| 功能/回归（自动化） | 每次提交（CI）+ 本地 verify.sh | 内联单测 |
| 安全（自动化） | 每次提交（CI） | audit/clippy + 安全回归测试 |
| 冒烟（托盘/单实例/信号） | 每次发布前 | scripts/smoke-linux.sh |
| 功能/易用性手工用例 | 每次发布前 | 本文档 1/6 节清单 |
| 性能 | 发布前（P-001/002/004），大版本全量 | 本文档第 2 节规程 |
| 兼容性矩阵 | 发布前（三平台），平台专项按需 | 本文档第 3 节矩阵 |
| 渗透/审计清单 | 发布前 | 本文档第 4.2 节 |
